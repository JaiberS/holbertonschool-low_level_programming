#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints alphabet
 * Return: always returns 0
*/

int main(void)
{
	int dec = 0;

	while (dec < 10)
	{
		printf("%i", dec);
		dec++;
	}
	return (0);
}
